import React from 'react';
import {
  Layout,
  Menu,
} from 'antd';

const { Header } = Layout;

function RHeader() {
  return (
    <Header>
      <Menu>
        men
      </Menu>
    </Header>
  );
}

export default RHeader;
